# `crate-env`
crate-env - is a `no_std`-compatible Rust crate, which allows you to retrieve your crate information in a structured
way, instead of relying on `env!` and `option_env!` macros.

# Install
To add `crate-env` to your project, use the following command:
```
cargo add crate-env
```

